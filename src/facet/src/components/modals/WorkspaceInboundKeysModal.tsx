// WorkspaceInboundKeysModal removed — stream and inbound keys are now managed
// per source artifact (vnd.agience.stream+json, vnd.agience.telegram+json, etc.).
// The key generate/rotate UX lives in each artifact's viewer.
export default function WorkspaceInboundKeysModal() {
  return null;
}
